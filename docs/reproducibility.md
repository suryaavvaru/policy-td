# Reproducibility

The repository stores curated code, schemas, summary tables, and figure source data. Large frozen runtime artifacts should be attached to a versioned release rather than committed directly to git.

Every public result should trace to a table, manifest, checksum, or script.
